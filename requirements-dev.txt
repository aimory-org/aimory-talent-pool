# =============================================================================
# Python Development Dependencies
# =============================================================================
# Install with: pip install -r requirements-dev.txt

# AWS SDK - matches Lambda runtime
boto3>=1.34.0
opensearch-py>=2.4.0     # Used by list_talents and sync_to_opensearch Lambdas

# Testing
pytest>=8.0.0
pytest-cov>=4.1.0        # Coverage reports
moto[all]>=5.0.0         # AWS service mocking

# Code quality
black>=24.0.0            # Formatter
ruff>=0.3.0              # Linter (replaces flake8, isort, etc.)
bandit>=1.7.0            # Security linter (OWASP / SAST)
bandit-sarif-formatter>=1.1.1  # SARIF output format plugin for bandit
pip-audit>=2.7.0         # Dependency vulnerability scanner (CVE/PyPI advisory DB)

# Type checking
mypy>=1.8.0
boto3-stubs[essential]   # Type hints for boto3
